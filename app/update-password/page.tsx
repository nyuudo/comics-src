import UpdateUserPassword from "@/components/feature/UpdateUserPassword";

export const metadata = {
  title: "Update Password",
};

export default function UpdatePassword() {
  return <UpdateUserPassword />;
}
